import React from "react";
import {Text, View, VStack} from "native-base";

const CreateAccountScreen = () => {
    return (
        <VStack pt={10}>
            <Text fontSize={32}>Connect to an existing account</Text>
        </VStack>
    );
};

export default CreateAccountScreen;