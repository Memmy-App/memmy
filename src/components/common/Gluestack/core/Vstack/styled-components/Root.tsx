import { View } from "react-native";
import { styled } from "../../styled";

export default styled(
  View,
  {
    flexDirection: "column",
    defaultProps: {
      space: "md",
    },
  },
  {}
);
