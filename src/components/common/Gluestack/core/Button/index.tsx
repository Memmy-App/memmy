import { createButton } from "@gluestack-ui/button";
import {
  Group,
  GroupHSpacer,
  GroupVSpacer,
  Icon,
  Root,
  Spinner,
  Text,
} from "./styled-components";

export const Button = createButton({
  Root,
  Text,
  Group,
  GroupHSpacer,
  GroupVSpacer,
  Spinner,
  Icon,
});
